# ============================================
# SDNCheck Docker Environment Configuration
# ============================================
# Copy this file to .env before running docker-compose
# All credentials should be changed for production deployments
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d
#
# For GitHub Actions/Cloud deployments:
#   - Set these as repository secrets or environment variables
#   - Never commit actual credentials to source control

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Database credentials
# ⚠️ SECURITY: Change these values for production!
DB_USER=sdn_user
DB_PASSWORD=CHANGE_THIS_PASSWORD
DB_NAME=sdn_database

# Database port (host port mapping)
DB_PORT=5432

# ============================================
# API SERVER CONFIGURATION
# ============================================

# API port (host port mapping)
API_PORT=8000

# Environment mode: development, staging, production
ENVIRONMENT=development

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# DATABASE CONNECTION POOL
# ============================================

# Connection pool size
DB_POOL_SIZE=5

# Max overflow connections
DB_MAX_OVERFLOW=10

# ============================================
# OPTIONAL: MONITORING CONFIGURATION
# ============================================
# Uncomment for Prometheus/Grafana monitoring stack

# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# GRAFANA_PASSWORD=admin

# ============================================
# OPTIONAL: REDIS CONFIGURATION
# ============================================
# Uncomment to enable Redis caching

# REDIS_PORT=6379
