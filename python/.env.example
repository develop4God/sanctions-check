# ============================================
# SDNCheck Environment Configuration
# ============================================
# Copy this file to .env and modify values as needed
# All credentials should be changed for production deployments
#
# For Docker/GitHub Actions deployments:
# - Set these as repository secrets or environment variables
# - Never commit actual credentials to source control
#
# For Docker Secrets (production):
# - Use Docker secrets for sensitive values like DB_PASSWORD
# - Reference: https://docs.docker.com/compose/use-secrets/

# ============================================
# DATABASE CONFIGURATION (Required)
# ============================================
# These values configure the PostgreSQL database connection

# Database host
# For Docker Compose: use service name 'db'
# For external database: use hostname or IP
DB_HOST=localhost

# Database port
DB_PORT=5432

# Database name
DB_NAME=sdn_database

# Database user
# ⚠️ SECURITY: Change in production!
DB_USER=sdn_user

# Database password
# ⚠️ SECURITY: Use a strong, unique password in production!
# For GitHub Actions: Set as a repository secret
DB_PASSWORD=CHANGE_THIS_PASSWORD

# Alternative: Full database URL (overrides individual settings above)
# Uncomment and set to use DATABASE_URL instead
# DATABASE_URL=postgresql://sdn_user:your_password@localhost:5432/sdn_database

# ============================================
# DATABASE CONNECTION POOL
# ============================================
# Connection pool settings for SQLAlchemy

# Maximum number of connections in the pool
DB_POOL_SIZE=5

# Maximum overflow connections (temporary connections beyond pool_size)
DB_MAX_OVERFLOW=10

# Connection timeout in seconds
DB_POOL_TIMEOUT=30

# Recycle connections after this many seconds
DB_POOL_RECYCLE=1800

# Enable SQL query logging (set to true for debugging)
DB_ECHO=false

# ============================================
# API SERVER CONFIGURATION
# ============================================

# API host address
# For Docker: use 0.0.0.0 to accept external connections
# For local development: use 127.0.0.1
API_HOST=0.0.0.0

# API port
API_PORT=8000

# Environment mode
# Options: development, staging, production
ENVIRONMENT=development

# Log level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# DATA CONFIGURATION
# ============================================

# Directory for sanctions data files
DATA_DIR=sanctions_data

# Path to configuration YAML file
CONFIG_PATH=config.yaml

# ============================================
# SECURITY CONFIGURATION
# ============================================

# Maximum file upload size in MB
MAX_UPLOAD_SIZE_MB=10

# API Key for authentication (required for production)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# ⚠️ SECURITY: Set a unique API key in production!
# API_KEY=your_secure_api_key_here

# CORS Configuration (comma-separated list of allowed origins)
# Example: http://localhost:3000,http://localhost:5173
# Leave empty to use default localhost origins
CORS_ORIGINS=

# ============================================
# OPTIONAL: REDIS CONFIGURATION
# ============================================
# Uncomment to enable Redis caching

# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0

# ============================================
# OPTIONAL: MONITORING CONFIGURATION
# ============================================
# Uncomment for Prometheus/Grafana monitoring

# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
# GRAFANA_PASSWORD=admin
